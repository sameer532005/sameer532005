name: generate animation

on:
  # run automatically every 24 hours
  schedule:
    - cron: "0 0 * * *-" # run at 00:00 UTC

  # allows to manually run the job at any time
  workflow_dispatch:
  
  # run on every push on the master branch
  push:
    branches:
    - master
    
jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      # generates <github_contribution_grid_snake.svg>
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg
            dist/github-snake-dark.svg?palette=github-dark
        env:
          # a github token is required to fetch the contribution calendar
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
          # set custom snake colors
          # --theme: light
          # snake_color: #1a1a1a (darkish grey)
          # dots_color: #ffffff (white)
          # --theme: dark
          # snake_color: #bf00ff (neon purple)
          # dots_color: #2a2a2a (dark grey)
          
      # push the content of <build_dir> to a branch
      # the content will be available at https://raw.githubusercontent.com/<github_user>/<repository>/<target_branch>/<file> , or as github page
      - name: push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
